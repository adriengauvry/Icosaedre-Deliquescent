import React from 'react';
import { Button, Typography, TextField } from '@material-ui/core';
import {} from './CardNewGame.scss';

class CardNewGame extends React.Component {
  

  render() {

    return (
      <div>
        {/* <CircularProgress size={this.state.progressSize} className='card__circle_progress' /> */}
      </div>
    );
  }
}

export default CardNewGame;
